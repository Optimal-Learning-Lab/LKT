% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lkt-model-online-calibration.R
\name{OnlineCalibrationModel}
\alias{OnlineCalibrationModel}
\title{Define the online calibration LKT model}
\usage{
OnlineCalibrationModel(
  alpha_start = 0,
  alpha_lower = 0,
  alpha_upper = 10,
  beta_start = "liblinear",
  beta_lower = -Inf,
  beta_upper = Inf,
  optimize_alpha = TRUE,
  optimize_beta = FALSE,
  clip_epsilon = 1e-06,
  denom_epsilon = clip_epsilon,
  subject_col = "Anon.Student.Id",
  fixed_online_coefficients = c("(Intercept)", "intercept"),
  update_strategy = c("all", "best_loss"),
  maxit = 100,
  factr = 1e+07
)
}
\arguments{
\item{alpha_start}{Initial learning-rate value.}

\item{alpha_lower}{Lower bound for the learning rate.}

\item{alpha_upper}{Upper bound for the learning rate.}

\item{beta_start}{Initial value for feature weights. Use \code{"liblinear"} to
initialize from the standard static LKT fit. Numeric scalars are recycled.}

\item{beta_lower}{Lower bound for feature weights. A scalar is recycled.}

\item{beta_upper}{Upper bound for feature weights. A scalar is recycled.}

\item{optimize_alpha}{Logical; optimize the learning rate when \code{TRUE}.}

\item{optimize_beta}{Reserved for future joint optimization of the initial
beta vector. The implemented model solves the initial beta vector globally
before optimizing \code{alpha}.}

\item{clip_epsilon}{Probability clipping value used before log likelihoods.}

\item{denom_epsilon}{Small denominator stabilizer for online updates.}

\item{subject_col}{Subject identifier column used for within-subject updates.}

\item{fixed_online_coefficients}{Coefficients that remain fixed during
within-subject online updates. By default, the global intercept is kept
fixed when present.}

\item{update_strategy}{Online update rule. \code{"all"} updates every eligible
active coefficient on the trial. \code{"best_loss"} tries each eligible
one-coefficient update and applies only the update that most reduces that
trial's immediate logistic loss.}

\item{maxit}{Maximum optimizer iterations.}

\item{factr}{Optimizer \code{factr} control value.}
}
\value{
A model adapter suitable for \code{LKT(model = ...)}.
}
\description{
Creates an LKT model adapter that first solves global feature
weights with the standard static LKT/LibLinear model, then uses those weights
as the starting beta vector for every subject. Within each subject, the full
beta vector is updated online after each observed response:
\code{beta[j, t + 1] = beta[j, t] + alpha * x[j, t] * (y[t] - p[t]) /
(x[j, t]^2 + denom_epsilon)} for each online-updated coefficient \code{j}.
With \code{alpha = 0}, this model reproduces the static global-beta
predictions.
}
\examples{
adapter <- OnlineCalibrationModel(maxit = 5)
adapter$name
}
