% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lkt-search.R
\name{LASSOLKTModel}
\alias{LASSOLKTModel}
\title{Fit a LASSO-selected LKT model}
\usage{
LASSOLKTModel(
  data,
  gridpars,
  allcomponents,
  preset = NA,
  presetint = T,
  allfeatures,
  specialcomponents = c(),
  specialfeatures = c(),
  specialpars = c(),
  target_n,
  removefeat = c(),
  removecomp = c(),
  test_fold = 1
)
}
\arguments{
\item{data}{Data frame or data table containing \code{Anon.Student.Id}, \code{CF..ansbin.}, component columns, and a \code{fold} column.}

\item{gridpars}{Numeric vector of parameter values used to materialize nonlinear feature candidates.}

\item{allcomponents}{Character vector of candidate component columns.}

\item{preset}{Optional preset model family: \code{"static"}, \code{"AFM"}, \code{"PFA"}, \code{"advanced"}, \code{"AFMLLTM"}, \code{"PFALLTM"}, or \code{"advancedLLTM"}.}

\item{presetint}{Logical; include preset intercept terms when \code{TRUE}.}

\item{allfeatures}{Character vector of candidate feature names.}

\item{specialcomponents}{Character vector of components paired one-to-one with \code{specialfeatures}.}

\item{specialfeatures}{Character vector of special feature names paired with \code{specialcomponents}.}

\item{specialpars}{Numeric vector of parameter values for \code{specialfeatures}.}

\item{target_n}{Desired number of nonzero model features used to choose a lambda from the fitted path.}

\item{removefeat}{Character vector of feature names to exclude from the crossed candidate list.}

\item{removecomp}{Character vector of component names paired with \code{removefeat} to exclude from the crossed candidate list.}

\item{test_fold}{Fold value used as the held-out test fold.}
}
\value{
A list containing training and test matrices, response vectors, the fitted \code{glmnet} object, AUC/RMSE/BIC by lambda, selected lambda index, predictions, selected coefficients, and selected feature summary.
}
\description{
Builds LKT feature matrices with \code{LASSOLKTData()} and fits a binomial \code{glmnet} path. This is a legacy public model-selection helper retained for vignette compatibility.
}
\examples{
\dontrun{
data(samplelkt)
samplelkt$CF..ansbin. <- as.integer(samplelkt$Outcome == "CORRECT")
samplelkt$fold <- rep(1:2, length.out = nrow(samplelkt))
lasso_model <- LASSOLKTModel(
  data = samplelkt,
  gridpars = c(0.25, 0.5),
  allcomponents = "KC..Default.",
  allfeatures = "lineafm",
  target_n = 2,
  test_fold = 1
)
names(lasso_model)
}
}
