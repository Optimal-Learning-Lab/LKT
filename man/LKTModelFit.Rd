% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lkt-model-interface.R
\name{LKTModelFit}
\alias{LKTModelFit}
\title{Create an LKT model fit result}
\usage{
LKTModelFit(
  model,
  pred,
  modelvs = NULL,
  fitstat = NULL,
  predictset = NULL,
  predictset2 = NULL
)
}
\arguments{
\item{model}{Fitted model object.}

\item{pred}{Numeric probability vector, one value per input row.}

\item{modelvs}{Optional coefficient matrix/data frame with a \code{coefficient} column.}

\item{fitstat}{Optional log likelihood or comparable fit statistic.}

\item{predictset}{Optional sparse model matrix.}

\item{predictset2}{Optional CSR model matrix used by existing LKT outputs.}
}
\value{
A model fit-result list.
}
\description{
Standardizes the fit-result object returned by LKT model adapters. \code{model} and \code{pred} are required. \code{pred} must be one finite probability per row in the shared feature input, strictly between 0 and 1. If \code{modelvs}, \code{fitstat}, or predictor matrices are omitted, LKT fills common defaults where possible.
}
\examples{
fit_result <- LKTModelFit(
  model = list(name = "constant"),
  pred = c(0.5, 0.5)
)
names(fit_result)
}
