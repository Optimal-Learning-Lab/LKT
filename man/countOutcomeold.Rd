% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lkt-feature-computation.R
\name{countOutcomeold}
\alias{countOutcomeold}
\title{Count prior outcomes within an index}
\usage{
countOutcomeold(data, index, response)
}
\arguments{
\item{data}{Data frame or data table containing an \code{Outcome} column.}

\item{index}{Grouping column name or data.table \code{by} expression that defines the history stream for each row.}

\item{response}{Outcome value to count, such as \code{"CORRECT"} or \code{"INCORRECT"}.}
}
\value{
Numeric vector of lagged cumulative counts, one value per row.
}
\description{
Legacy exported helper that counts previous rows whose \code{Outcome} value equals \code{response} within each \code{index} group. It is retained for compatibility with older examples; new package code uses internal helpers.
}
\examples{
data(samplelkt)
data.table::setDT(samplelkt)
samplelkt$CF..ansbin. <- as.integer(samplelkt$Outcome == "CORRECT")
samplelkt$history_index <- paste(samplelkt$KC..Default., samplelkt$Anon.Student.Id)
head(countOutcomeold(samplelkt, "history_index", "CORRECT"))
}
