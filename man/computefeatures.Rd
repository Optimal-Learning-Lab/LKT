% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lkt-feature-computation.R
\name{computefeatures}
\alias{computefeatures}
\title{Compute an LKT feature vector}
\usage{
computefeatures(data, feat, par1, par2, index, index2, par3, par4, par5, fcomp)
}
\arguments{
\item{data}{Data frame or data table after LKT component preparation.}

\item{feat}{Feature name to compute.}

\item{par1}{First nonlinear feature parameter.}

\item{par2}{Second nonlinear feature parameter.}

\item{index}{Student-by-component index vector.}

\item{index2}{Component-level index vector.}

\item{par3}{Third nonlinear feature parameter.}

\item{par4}{Fourth nonlinear feature parameter.}

\item{par5}{Fifth nonlinear feature parameter.}

\item{fcomp}{Component column name used by features that read component-level spacing or numeric data.}
}
\value{
A vector suitable for use as one model-matrix input column.
}
\description{
Computes one feature vector for a prepared LKT component context. This is a legacy public helper used by examples and advanced workflows; most users should call \code{LKT()} instead.
}
\examples{
data(samplelkt)
data.table::setDT(samplelkt)
samplelkt$CF..ansbin. <- as.integer(samplelkt$Outcome == "CORRECT")
prepared <- computeSpacingPredictors(samplelkt, "KC..Default.")
prepared$history_index <- paste(prepared$KC..Default., prepared$Anon.Student.Id)
prepared$index <- prepared$history_index
prepared$indexcomp <- prepared$KC..Default.
prepared$cor <- countOutcomeold(prepared, "history_index", "CORRECT")
prepared$icor <- countOutcomeold(prepared, "history_index", "INCORRECT")
head(computefeatures(
  prepared, "lineafm", 0, 0,
  prepared$index, prepared$indexcomp, 0, 0, 0, "KC..Default."
))
}
