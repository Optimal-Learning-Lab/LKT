% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lkt-hdi.R
\name{LKT_HDI}
\alias{LKT_HDI}
\title{Estimate coefficient HDIs by bootstrap resampling}
\usage{
LKT_HDI(
  dat,
  n_boot,
  n_students,
  comps,
  feats,
  conns = rep("+", max(1, length(comps) - 1)),
  ints = NA,
  fixeds,
  get_hdi = TRUE,
  cred_mass = 0.95
)
}
\arguments{
\item{dat}{Data frame or data table containing trial data for \code{LKT()}.}

\item{n_boot}{Number of bootstrap subsamples to fit.}

\item{n_students}{Number of students per bootstrap subsample.}

\item{comps}{Character vector of components passed to \code{LKT()}.}

\item{feats}{Character vector of features passed to \code{LKT()}.}

\item{conns}{Character vector of formula connectors passed to \code{LKT()}.}

\item{ints}{Optional interactions passed to \code{LKT()}.}

\item{fixeds}{Fixed nonlinear parameters passed to \code{LKT()}.}

\item{get_hdi}{Logical compatibility argument. The current implementation always returns \code{coef_hdi}.}

\item{cred_mass}{Credibility mass used for HDI width.}
}
\value{
A list containing bootstrap coefficient replicates (\code{par_reps}), the full-data model (\code{mod_full}), and coefficient intervals (\code{coef_hdi}).
}
\description{
Fits a full LKT model and repeated student-level bootstrap samples, then summarizes coefficient intervals with \code{HDInterval}. This helper is computation-heavy and is retained as a legacy public interpretation aid.
}
\examples{
\donttest{
data(samplelkt)
hdi_result <- LKT_HDI(
  samplelkt,
  n_boot = 2,
  n_students = 2,
  comps = "KC..Default.",
  feats = "intercept",
  fixeds = numeric(0)
)
names(hdi_result)
}
}
