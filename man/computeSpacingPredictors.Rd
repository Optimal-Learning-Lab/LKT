% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lkt-feature-computation.R
\name{computeSpacingPredictors}
\alias{computeSpacingPredictors}
\title{Compute spacing predictors for LKT features}
\usage{
computeSpacingPredictors(data, KCs)
}
\arguments{
\item{data}{Data frame or data table containing \code{Anon.Student.Id}, \code{CF..ansbin.}, and each component named in \code{KCs}. Requires either \code{CF..reltime.} or \code{Duration..sec.}.}

\item{KCs}{Character vector of component columns for which spacing predictors should be computed.}
}
\value{
The input data with added spacing-history columns for each component: \code{spacing}, \code{relspacing}, \code{prev}, \code{meanspacing}, \code{relmeanspacing}, and \code{spacinglagged} suffixes.
}
\description{
Adds repetition-spacing history columns for one or more component columns. These columns are required by recency, spacing, and forgetting features in \code{LKT()}. If \code{CF..reltime.} or \code{CF..Time.} is missing, the function derives it from \code{Duration..sec.} when possible.
}
\examples{
data(samplelkt)
data.table::setDT(samplelkt)
samplelkt$CF..ansbin. <- as.integer(samplelkt$Outcome == "CORRECT")
spacing_data <- computeSpacingPredictors(samplelkt, "KC..Default.")
head(spacing_data$KC..Default.spacing)
}
